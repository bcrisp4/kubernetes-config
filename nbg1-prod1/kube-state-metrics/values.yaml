kube-state-metrics:
  # Enable Prometheus metrics scraping via pod annotations (default: true)
  prometheusScrape: true

  resources:
    requests:
      cpu: 10m
      memory: 32Mi
    limits:
      memory: 128Mi
