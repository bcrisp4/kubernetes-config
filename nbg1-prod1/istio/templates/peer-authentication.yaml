# Enforce STRICT mTLS for all services in the mesh
# This ensures all service-to-service communication uses mutual TLS
apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: default
  namespace: istio
spec:
  mtls:
    mode: STRICT
