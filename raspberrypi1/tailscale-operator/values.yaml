tailscale-operator:
  ingressClass:
    enabled: true

  installCRDs: true

  operatorConfig:
    defaultTags:
      - tag:k8s-operator
    resources:
      requests:
        cpu: 25m
        memory: 100Mi
      limits:
        memory: 100Mi
